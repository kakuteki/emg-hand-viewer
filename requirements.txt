# 手モデルの表示に必要なもの
numpy>=1.20.0
PyQt5>=5.15.0
pyqtgraph>=0.12.0
PyOpenGL>=3.1.0

# 学習済みモデル（.pth）を使う場合のみ必要
# pip install torch
#
# Myo Armbandを実機で使う場合のみ必要
# pip install pyomyo
#
# 開発（試験・書式チェック）
# pip install pytest ruff
